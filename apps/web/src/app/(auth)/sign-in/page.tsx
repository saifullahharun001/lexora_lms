export default function SignInPage() {
  return (
    <div>
      <p className="text-sm uppercase tracking-[0.35em] text-amber-400">Auth</p>
      <h1 className="mt-4 text-3xl font-semibold text-stone-50">Sign-in shell</h1>
      <p className="mt-4 text-sm leading-6 text-stone-400">
        Authentication entry points will live here. This foundation keeps routing,
        validation dependencies, and provider setup in place without implementing
        business logic yet.
      </p>
    </div>
  );
}

